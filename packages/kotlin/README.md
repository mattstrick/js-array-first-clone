# array-first (Kotlin)

> Get the first element or first n elements of a list.

## Usage

```kotlin
import com.mattstrick.arrayfirst.first

val list = listOf(1, 2, 3, 4, 5)
first(list)      // 1
first(list, 3)  // [1, 2, 3]
```

## Build

```bash
./gradlew build
```

## License

MIT
